server {
    listen 80;
    listen [::]:80;

    server_name YOUR_DOMAIN.com www.YOUR_DOMAIN.com;

    root /var/www/YOUR_DOMAIN.com/public_html;
    index index.html;

    # Security headers
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-XSS-Protection "1; mode=block" always;

    # Serve static files
    location / {
        try_files $uri $uri/ =404;
    }

    # Cache static assets
    location ~* \.(jpg|jpeg|png|gif|svg|ico|css|js|woff2)$ {
        expires 30d;
        add_header Cache-Control "public, immutable";
    }

    # Deny access to hidden files
    location ~ /\. {
        deny all;
    }

    # Deny access to docs and deploy directories
    location ~ ^/(docs|deploy)/ {
        deny all;
    }

    # Error pages
    error_page 404 /404.html;
    error_page 500 502 503 504 /50x.html;

    access_log /var/log/nginx/YOUR_DOMAIN-access.log;
    error_log /var/log/nginx/YOUR_DOMAIN-error.log;

    # Redirect to HTTPS (uncomment after SSL is configured)
    # return 301 https://$host$request_uri;
}

# HTTPS Server Block (uncomment after certbot has generated certificates)
# server {
#     listen 443 ssl http2;
#     listen [::]:443 ssl http2;
#
#     server_name YOUR_DOMAIN.com www.YOUR_DOMAIN.com;
#
#     root /var/www/YOUR_DOMAIN.com/public_html;
#     index index.html;
#
#     ssl_certificate /etc/letsencrypt/live/YOUR_DOMAIN.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/YOUR_DOMAIN.com/privkey.pem;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#
#     add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
#
#     location / {
#         try_files $uri $uri/ =404;
#     }
#
#     location ~* \.(jpg|jpeg|png|gif|svg|ico|css|js|woff2)$ {
#         expires 30d;
#         add_header Cache-Control "public, immutable";
#     }
#
#     location ~ /\. {
#         deny all;
#     }
# }
